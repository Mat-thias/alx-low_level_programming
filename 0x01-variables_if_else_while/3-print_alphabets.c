#include <stdio.h>

/**
 * main - main block
 * Description: Get a random number and check its last digit, compare it with 5
 * Return: 0
 */

int main (void)
{
  char cha;

  for (cha = 'a'; cha <= 'z'; cha++)
  {
    putchar(cha);
  }
  for (cha = 'A'; ;cha <= 'z'; cha++)
  {
    putchar(cha);
  }
  putchar(\n);

  return (0);

}
